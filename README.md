# Arbitrary-Precision-Calculator-APC-
Arbitrary Precision Calculator in C — A data structures-based project implementing big number arithmetic using linked lists. Supports operations like addition, subtraction, multiplication, division, modulus, and power on numbers of arbitrary length with optimized handling for zero and corner cases.
